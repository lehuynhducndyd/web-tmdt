<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hóa đơn - <%= order._id %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .invoice-container {
            max-width: 800px;
            margin: 30px auto;
            padding: 40px;
            background-color: #fff;
            border: 1px solid #dee2e6;
            border-radius: 0.25rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, .15);
        }

        .invoice-header {
            border-bottom: 2px solid #000;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .invoice-footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            font-style: italic;
            color: #6c757d;
        }

        .print-controls {
            text-align: center;
            margin: 20px 0;
        }

        @media print {
            body {
                background-color: #fff;
            }

            .print-controls {
                display: none;
            }

            .invoice-container {
                margin: 0;
                padding: 10px;
                border: none;
                box-shadow: none;
                max-width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="print-controls">
        <button class="btn btn-secondary" onclick="window.history.back()">
            <i class="fas fa-arrow-left"></i> Quay lại
        </button>
        <button class="btn btn-primary" onclick="window.print()">
            <i class="fas fa-print"></i> In hóa đơn
        </button>
    </div>

    <div class="invoice-container">
        <header class="invoice-header">
            <div class="row align-items-center">
                <div class="col-sm-6">
                    <h2 class="fw-bold">Tripple D Shop</h2>
                    <p class="mb-0">123 Đường ABC, Phường XYZ, Quận Gì Đó, TP. HCM</p>
                    <p class="mb-0">Điện thoại: 0123 456 789</p>
                </div>
                <div class="col-sm-6 text-sm-end">
                    <h2 class="text-uppercase fw-bolder mb-1">Hóa đơn bán hàng</h2>
                    <p class="mb-0">Mã đơn hàng: <strong>#<%= order._id %></strong></p>
                    <p class="mb-0">Ngày đặt: <%= new Date(order.createdAt).toLocaleDateString('vi-VN') %>
                    </p>
                </div>
            </div>
        </header>

        <main>
            <div class="row mb-4">
                <div class="col-sm-12">
                    <h5 class="fw-bold">Thông tin khách hàng</h5>
                    <p class="mb-1"><strong>Tên khách hàng:</strong>
                        <%= order.customer.name %>
                    </p>
                    <p class="mb-1"><strong>Số điện thoại:</strong>
                        <%= order.shippingAddress.phone %>
                    </p>
                    <p class="mb-1"><strong>Địa chỉ giao hàng:</strong>
                        <%= `${order.shippingAddress.street}, ${order.shippingAddress.commune},
                            ${order.shippingAddress.province}` %>
                    </p>
                    <p class="mb-1"><strong>Phương thức thanh toán:</strong>
                        <%= order.paymentMethod %>
                    </p>
                </div>
            </div>

            <h5 class="fw-bold">Chi tiết sản phẩm</h5>
            <table class="table table-bordered">
                <thead class="table-light">
                    <tr>
                        <th scope="col" class="text-center">STT</th>
                        <th scope="col">Sản phẩm</th>
                        <th scope="col" class="text-end">Đơn giá</th>
                        <th scope="col" class="text-center">Số lượng</th>
                        <th scope="col" class="text-end">Thành tiền</th>
                    </tr>
                </thead>
                <tbody>
                    <% order.items.forEach((item, index)=> { %>
                        <tr>
                            <td class="text-center">
                                <%= index + 1 %>
                            </td>
                            <td>
                                <% if (item.product) { %>
                                    <%= item.product.name %>
                                        <% if (item.variant) { %>
                                            <br>
                                            <small class="text-muted">
                                                <% if (item.variant.ram) { %>RAM: <%= item.variant.ram %>
                                                        <% } %>
                                                            <% if (item.variant.storage) { %> - <%= item.variant.storage
                                                                    %>
                                                                    <% } %>
                                                                        <% if (item.variant.color) { %> - Màu: <%=
                                                                                item.variant.color %>
                                                                                <% } %>
                                            </small>
                                            <% } %>
                                                <% } else { %>
                                                    Sản phẩm không tồn tại
                                                    <% } %>
                            </td>
                            <td class="text-end">
                                <%= new Intl.NumberFormat('vi-VN', { style: 'currency' , currency: 'VND'
                                    }).format(item.price) %>
                            </td>
                            <td class="text-center">
                                <%= item.quantity %>
                            </td>
                            <td class="text-end">
                                <%= new Intl.NumberFormat('vi-VN', { style: 'currency' , currency: 'VND'
                                    }).format(item.price * item.quantity) %>
                            </td>
                        </tr>
                        <% }) %>
                            <tr class="fw-bold">
                                <td colspan="4" class="text-end">Tổng cộng</td>
                                <td class="text-end text-danger">
                                    <%= new Intl.NumberFormat('vi-VN', { style: 'currency' , currency: 'VND'
                                        }).format(order.totalAmount) %>
                                </td>
                            </tr>
                </tbody>
            </table>
        </main>

        <footer class="invoice-footer">
            <p>Cảm ơn quý khách đã mua hàng tại Tripple D Shop!</p>
            <p>www.trippledshop.com</p>
        </footer>
    </div>

</body>

</html>